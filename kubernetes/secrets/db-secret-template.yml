apiVersion: v1
kind: Secret
metadata:
  name: db-credentials
  labels:
    app: devops-app
type: Opaque
stringData:
  username: admin
  password: ${DB_PASSWORD}
  connection-string: "postgresql://admin:${DB_PASSWORD}@${DB_HOST}:5432/appdb"
